<UserControl x:Class="OpenSteam.Views.OnlineLua"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True">
    <UserControl.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="10" BorderThickness="1" BorderBrush="#33FFFFFF"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#44FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E01E37"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Canvas IsHitTestVisible="False"/>

        <StackPanel VerticalAlignment="Center" Margin="30">
            <TextBlock Text="STEAM LUA" Foreground="White" FontSize="34" FontWeight="ExtraBold" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#E01E37" Opacity="0.5" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="ONLINE STORE" Foreground="#E01E37" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,-5,0,30" />

            <Border Background="#15FFFFFF" CornerRadius="15" Padding="20" BorderThickness="1" BorderBrush="#1AFFFFFF">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <TextBox x:Name="SearchBox" Background="#0D1117" Foreground="White" FontSize="16" Padding="15,12" BorderBrush="#33FFFFFF" BorderThickness="1" SelectionBrush="#E01E37" CaretBrush="White">
                            <TextBox.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <TextBlock IsHitTestVisible="False" Text="Enter AppID or Name..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="18,0,0,0" Foreground="#44FFFFFF" FontSize="16">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <Button x:Name="ButtonSearch" Height="50" Style="{StaticResource ModernButtonStyle}" Click="Search_Click" Width="270">
                        <Button.Background>
                            <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                <GradientStop Color="#E01E37" Offset="0"/>
                                <GradientStop Color="#B81430" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Grid>
                            <TextBlock x:Name="ButtonText" Text="Download Game" VerticalAlignment="Bottom"/>
                            <ProgressBar x:Name="ButtonProgress" IsIndeterminate="True" Width="100" Height="4" Visibility="Collapsed" Foreground="White" Background="Transparent" BorderThickness="0"/>
                        </Grid>
                    </Button>
                </StackPanel>
            </Border>

            <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="12"><Run Text="Tip:" Foreground="#E01E37" FontWeight="Bold"/><Run Text=" "/><Run Text=" Use SteamDB to find the correct AppID." Foreground="#88FFFFFF"/></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
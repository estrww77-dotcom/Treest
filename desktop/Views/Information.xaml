<UserControl x:Class="OpenSteam.Views.Information"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True">
    <Grid>
        <Canvas IsHitTestVisible="False"/>
        <StackPanel Margin="30">
            <TextBlock Text="RedSea Framework" Foreground="White" FontSize="28" FontWeight="ExtraBold" Margin="0,0,0,5">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="8" Color="#E01E37" ShadowDepth="0" Opacity="0.4"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock x:Name="InfoVersion" Text="Version 2.1 Beta" Foreground="#66FFFFFF" FontSize="12" Margin="0,0,0,25"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="380">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" FontSize="14" LineHeight="22" Margin="0,0,0,20"><Run Text="RedSea is an open-source tool designed to enhance user freedom within the Steam ecosystem. It acts as a compatibility and patching layer that allows the injection of advanced features."/></TextBlock>
                    <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" BorderThickness="1" BorderBrush="#1AFFFFFF">
                        <StackPanel>
                            <TextBlock Text="Dev" Foreground="#E01E37" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                            <TextBlock TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13" Text="Open source project — created by Jbrequi (Abrahamqb) . Github: https://github.com/Abrahamqb/OpenSteam/"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="🛠️ Core Features" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,20,0,12"/>
                    <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#1AFFFFFF">
                        <StackPanel>
                            <TextBlock Text="Library Jailbreak" Foreground="#E01E37" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                            <TextBlock Text="Unlocks the Steam library, allowing you to play games natively, including titles loaded via Lua." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#1AFFFFFF">
                        <StackPanel>
                            <TextBlock Text="LuaLoader" Foreground="#E01E37" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                            <TextBlock Text="Loads Lua files from external servers to access non-DRM titles. Note: Online play requires an Online Fix patch." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
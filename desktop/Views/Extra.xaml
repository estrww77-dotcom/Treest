<UserControl x:Class="OpenSteam.Views.Extra"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True">
    <Grid>
        <Canvas IsHitTestVisible="False"/>
        <StackPanel Margin="30">
            <TextBlock Text="RedSea Extras" Foreground="White" FontSize="28" FontWeight="ExtraBold" Margin="0,0,0,5">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="8" Color="#E01E37" ShadowDepth="0" Opacity="0.4"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="Useful tools and bypasses" Foreground="#66FFFFFF" FontSize="12" Margin="0,0,0,25"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="380">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" FontSize="14" LineHeight="22" Margin="0,0,0,20"><Run Text="RedSea Extras are tools, bypasses, etc., useful for your games or your Steam environment. Use them responsibly."/></TextBlock>
                    <Border Style="{StaticResource SettingCardStyle}">
                        <StackPanel>
                            <TextBlock Text="SteamCMD (Official)" Foreground="#FF1FFF00" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" MouseDown="Steamcmd" Cursor="Hand"/>
                            <TextBlock Text="SteamCMD (Steam Console) is the lightweight, official, command-line-based version of the Steam client." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SettingCardStyle}">
                        <StackPanel>
                            <TextBlock Text="NLGL =&gt; RockstarLauncher Bypass" Foreground="#FFF3FF00" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" MouseDown="nlgl" Cursor="Hand"/>
                            <TextBlock Text="Nightlight Game Launcher unlocks the possibility of playing any Rockstar Games title." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SettingCardStyle}">
                        <StackPanel>
                            <TextBlock Text="CreamInstaller =&gt; Unlocks any DLC" Foreground="#FFF3FF00" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" MouseDown="craminstaller" Cursor="Hand"/>
                            <TextBlock Text="Automatic DLC Unlocker Installer and Configuration Generator." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SettingCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Online-fix.me =&gt; Online Games Bypass" Foreground="#FFF3FF00" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" MouseDown="onlinefix" Cursor="Hand"/>
                            <TextBlock Text="Provides patches needed to play online multiplayer video games." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SettingCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Steam Achievement Manager" Foreground="Red" FontWeight="Bold" FontSize="14" Margin="0,0,0,5" MouseDown="steamachievementmanager" Cursor="Hand"/>
                            <TextBlock Text="Manage achievements and statistics in Steam. Use with caution." TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="13"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>